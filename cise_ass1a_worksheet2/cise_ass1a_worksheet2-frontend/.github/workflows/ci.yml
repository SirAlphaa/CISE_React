name: CI_action 

on: 
  push: 
    branches: [main] 
  pull_request: 
    branches: [main] 

jobs: 
  build: 
    runs-on: ubuntu-latest 

    strategy: 
      matrix: 
        node-version: [14.x] 

    steps: 
      - name: Checkout repository 
        uses: actions/checkout(ilv2 

      - name: Set up Node.js ${{ matrix.node-version 
        uses: actions/setup-node@vi 
        with: 
          node-version: PH •atrix.node-version 

      - name: Install dependencies 
        run: npm install 

      - name: Run the tests 
        run: nom test 

      - name: Build
        run: npm run build 